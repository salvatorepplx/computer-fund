# Reference material (study only, not the Fund's own code)

`autoresearch/` is a verbatim copy of Sal's prior self-improving research skill, vendored here so Teammate can mine it directly. See `../runs/AUTORESEARCH_TEARDOWN.md` for Computer's critical extraction of what transfers. Do NOT run autoresearch's scripts as-is; it has a different architecture (paper-only, human-confirm deploy, 13-role swarm). Take patterns, not code.
